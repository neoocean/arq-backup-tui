# API reference

Auto-generated by `scripts/build_api_docs.py`. Install [pdoc](https://pdoc.dev) for a richer browseable version under the same path.

## `arq_writer`

### `arq_writer.acl`

ACL capture + restore.

- **`def apply_acl(path, blob: 'bytes', *, callback: 'Optional[Callable[..., None]]' = None) -> 'bool'`**
  Apply an ACL blob captured by :func:`capture_acl`.

- **`def capture_acl(path, *, callback: 'Optional[Callable[..., None]]' = None) -> 'bytes'`**
  Read the ACL of ``path`` + return its on-disk bytes.

- **`def has_acl_support() -> 'bool'`**
  True iff the host has the platform's ACL CLI available.

### `arq_writer.arq_chunker_params`

Reverse-engineered chunker parameters for Arq.app v7.

- **`def install() -> 'None'`**
  Register Arq.app v7 parameters as the canonical (3, True) chunker.

### `arq_writer.backup`

High-level backup orchestrator.

- **`class Backup(dest_root: 'Path', encryption_password: 'str', *, backup_name: 'str' = 'TUI backup', computer_name: 'Optional[str]' = None, plan_name: 'Optional[str]' = None, computer_uuid: 'Optional[str]' = None, plan_uuid: 'Optional[str]' = None, encryption_key: 'Optional[bytes]' = None, hmac_key: 'Optional[bytes]' = None, blob_id_salt: 'Optional[bytes]' = None, openssl_path: 'str' = 'openssl', use_packs: 'bool' = False, max_pack_bytes: 'int' = 10485760, large_blob_threshold: 'int' = 256000, chunker_config: 'Optional[ChunkerConfig]' = None, dedup_against_existing: 'bool' = False, max_file_bytes: 'Optional[int]' = None, exclusions=None, backend=None, callback: 'Optional[ProgressCb]' = None, tree_version: 'int' = 3) -> 'None'`**
  Stateful backup writer.

- **`class BackupCancelled(...)`**
  Raised inside the writer when ``Backup.cancel()`` is invoked.

- **`class BackupResult(dest_root: 'Path', computer_uuid: 'str', plan_uuid: 'str', folder_uuid: 'str', backuprecord_path: 'Path', files_written: 'int' = 0, trees_written: 'int' = 0, files_with_errors: 'int' = 0, bytes_plaintext: 'int' = 0, bytes_on_disk: 'int' = 0, blob_ids: 'List[str]' = <factory>, started_at: 'float' = 0.0, finished_at: 'float' = 0.0) -> None`**
  Outcome of a single ``build_backup`` run.

- **`def build_backup(source: 'Path', dest_root: 'Path', encryption_password: 'str', *, backup_name: 'str' = 'TUI backup', folder_name: 'Optional[str]' = None, callback: 'Optional[ProgressCb]' = None, openssl_path: 'str' = 'openssl', computer_uuid: 'Optional[str]' = None, plan_uuid: 'Optional[str]' = None, folder_uuid: 'Optional[str]' = None, use_packs: 'bool' = False, max_pack_bytes: 'int' = 10485760, large_blob_threshold: 'int' = 256000, chunker_config: 'Optional[ChunkerConfig]' = None, dedup_against_existing: 'bool' = False, max_file_bytes: 'Optional[int]' = None, exclusions=None, use_apfs_snapshot: 'bool' = False, tree_version: 'int' = 3) -> 'BackupResult'`**
  One-shot convenience wrapper: full plan init + single folder.

### `arq_writer.backuprecord`

Build the ``backuprecord`` file.

- **`def blobloc_to_dict(loc: 'BlobLoc') -> 'Dict[str, Any]'`**

- **`def build_backuprecord_arqo(record_dict: 'Dict[str, Any]', *, encryption_key: 'bytes', hmac_key: 'bytes', openssl_path: 'str' = 'openssl', session_key: 'Optional[bytes]' = None, data_iv: 'Optional[bytes]' = None, master_iv: 'Optional[bytes]' = None) -> 'bytes'`**
  Pipeline: plist -> LZ4 wrap -> ARQO. Returns on-disk bytes.

- **`def build_backuprecord_dict(*, backup_folder_uuid: 'str', backup_plan_uuid: 'str', backup_plan_dict: 'Dict[str, Any]', root_node: 'Node', local_path: 'str', local_mount_point: 'str' = '/', relative_path: 'str' = '', arq_version: 'str' = '0.1.0', creation_date: 'Optional[float]' = None, computer_os_type: 'int' = 1, storage_class: 'str' = 'STANDARD', volume_name: 'str' = '', version: 'int' = 100, disk_identifier: 'str' = 'ROOT') -> 'Dict[str, Any]'`**
  Assemble the top-level dict written into the backuprecord plist.

- **`def node_to_dict(node: 'Node') -> 'Dict[str, Any]'`**
  Render a ``Node`` as the dict shape the spec shows in the example.

- **`def parse_backuprecord(plain: 'bytes') -> 'Dict[str, Any]'`**
  Parse a decrypted backuprecord payload, accepting either
Apple's binary plist (what our writer used to produce, before
the JSON default) or UTF-8 JSON (what Arq.app actually emits).

- **`def serialize_backuprecord(record: 'Dict[str, Any]', *, fmt: 'str' = 'json') -> 'bytes'`**
  Serialize the backuprecord dict for on-disk storage.

### `arq_writer.buzhash_re_cli`

CLI front-end for the Buzhash parameter RE workflow.

- **`def main(argv: 'Optional[List[str]]' = None) -> 'int'`**

### `arq_writer.chunker`

Content-defined chunker (Buzhash / cyclic polynomial rolling hash).

- **`class Buzhash(config: 'Optional[ChunkerConfig]' = None) -> 'None'`**
  Stateful Buzhash rolling-hash chunker.

- **`class ChunkerConfig(window_size: 'int' = 48, boundary_bits: 'int' = 15, min_chunk_size: 'int' = 4096, max_chunk_size: 'int' = 1048576, table: 'Optional[tuple]' = None) -> None`**
  Tuning knobs for :class:`Buzhash`. All defaults are
arbitrary-but-reasonable; change them only if you have a
specific dedup target in mind.

- **`def chunk_bytes(data: 'bytes', *, config: 'Optional[ChunkerConfig]' = None) -> 'Iterable[bytes]'`**
  Convenience wrapper. Equivalent to ``Buzhash(config).chunk(data)``.

- **`def chunker_for_arq(chunker_version: 'int' = 3, use_buzhash: 'bool' = True) -> 'ChunkerConfig'`**
  Return the registered ChunkerConfig for an Arq variant, or
the generic default if unregistered.

- **`def known_arq_variants() -> 'List[Tuple[int, bool]]'`**
  List currently-registered (chunker_version, use_buzhash) pairs.

- **`def register_arq_chunker(chunker_version: 'int', use_buzhash: 'bool', config: 'ChunkerConfig') -> 'None'`**
  Plug in a (chunkerVersion, useBuzhash) → ChunkerConfig binding.

### `arq_writer.chunker_oracle`

Falsification harness for reverse-engineered chunker parameters.

- **`class ChunkOracleReport(match: 'bool', input_sha256: 'str', input_length: 'int', expected_chunk_count: 'int', observed_chunk_count: 'int', expected_lengths: 'List[int]' = <factory>, observed_lengths: 'List[int]' = <factory>, matching_prefix: 'int' = 0, first_divergence_offset: 'Optional[int]' = None, notes: 'List[str]' = <factory>) -> None`**
  Outcome of comparing our chunker against an external length list.

- **`def compare_chunking(input_bytes: 'bytes', observed_lengths: 'Sequence[int]', *, config: 'Optional[ChunkerConfig]' = None) -> 'ChunkOracleReport'`**
  Run our chunker on ``input_bytes`` and compare against
``observed_lengths`` (the chunk-length sequence Arq.app produced
for the same input).

### `arq_writer.cli`

Standalone CLI: ``python -m arq_writer create <src> --dest <dest>``.

- **`def main(argv: 'Optional[List[str]]' = None) -> 'int'`**

### `arq_writer.crypto_write`

Write-side crypto: ARQO encoder + ``encryptedkeyset.dat`` writer.

- **`def aes_256_cbc_encrypt(key: 'bytes', iv: 'bytes', plaintext: 'bytes', *, openssl_path: 'str' = 'openssl') -> 'bytes'`**
  AES-256-CBC + PKCS7-pad encrypt via the openssl CLI.

- **`def build_encrypted_keyset(password: 'str', encryption_key: 'bytes', hmac_key: 'bytes', blob_id_salt: 'bytes', *, salt: 'Optional[bytes]' = None, iv: 'Optional[bytes]' = None, openssl_path: 'str' = 'openssl') -> 'bytes'`**
  Build an ``encryptedkeyset.dat`` payload.

- **`def build_encrypted_object(plaintext: 'bytes', encryption_key: 'bytes', hmac_key: 'bytes', *, session_key: 'Optional[bytes]' = None, data_iv: 'Optional[bytes]' = None, master_iv: 'Optional[bytes]' = None, openssl_path: 'str' = 'openssl') -> 'bytes'`**
  Build a single-object ``EncryptedObject`` (ARQO).

- **`def compute_blob_id(blob_id_salt: 'bytes', plaintext: 'bytes') -> 'str'`**
  SHA-256 hex blob identifier — Arq 7 content addressing.

- **`def rotate_keyset_password(keyset_blob: 'bytes', *, old_password: 'str', new_password: 'str', openssl_path: 'str' = 'openssl') -> 'bytes'`**
  Re-encrypt an ``encryptedkeyset.dat`` payload under a new
password without changing the master keys.

### `arq_writer.dedup`

Cross-run deduplication seeding.

- **`def find_latest_backuprecord(dest_root: 'Path', computer_uuid: 'str') -> 'Optional[Path]'`**
  Return the most recently created backuprecord across every
folder under ``<dest>/<cu>/backupfolders/.../backuprecords/``,
or ``None`` if the destination has no records.

- **`def find_latest_backuprecord_per_folder(dest_root: 'Path', computer_uuid: 'str', *, backend=None) -> 'Dict[str, Path]'`**
  Return ``{folder_uuid: latest_backuprecord_path}`` for every
folder under the given computer.

- **`def seed_existing_destination(dest_root: 'Path', computer_uuid: 'str', cache: 'Dict[str, BlobLoc]', *, encryption_key: 'Optional[bytes]' = None, hmac_key: 'Optional[bytes]' = None, backend=None) -> 'Dict[str, Any]'`**
  Best-effort full seed: standalone-scan + (if keys available)
walk every folder's most recent backuprecord.

- **`def seed_from_backuprecord(rec_path: 'Path', cache: 'Dict[str, BlobLoc]', *, encryption_key: 'bytes', hmac_key: 'bytes', dest_root: 'Optional[Path]' = None, openssl_path: 'str' = 'openssl', backend=None) -> 'int'`**
  Decrypt the backuprecord at ``rec_path``, walk its embedded
Node and every reachable Tree blob, and seed ``cache`` with
every referenced ``BlobLoc``.

- **`def seed_from_standardobjects(dest_root: 'Path', computer_uuid: 'str', cache: 'Dict[str, BlobLoc]', *, backend=None) -> 'int'`**
  Scan ``standardobjects/`` and populate ``cache`` with one
BlobLoc per existing file.

### `arq_writer.disk_estimator`

Pre-backup disk-usage estimator.

- **`class DiskEstimate(source_bytes: 'int' = 0, estimated_dest_bytes: 'int' = 0, free_space_bytes: 'int' = 0, will_fit: 'bool' = True, safety_margin_factor: 'float' = 1.0, notes: 'str' = '') -> None`**
  Output of :func:`estimate_destination_size`.

- **`def estimate_destination_size(source_bytes: 'int', dest_path: 'Path', *, compression_ratio: 'float' = 1.2, safety_factor: 'float' = 1.1) -> 'DiskEstimate'`**
  Estimate whether ``dest_path`` has enough free space for
a fresh backup of ``source_bytes``.

- **`def estimate_for_plan(plan, *, compression_ratio: 'float' = 1.2, safety_factor: 'float' = 1.1) -> 'DiskEstimate'`**
  Convenience: walk every source in a Plan + sum sizes +
estimate against the plan's primary destination.

### `arq_writer.dry_run`

Dry-run a backup plan: walk the source + apply the plan's
exclusion + size rules + report what WOULD be backed up, without
encrypting or writing any blob.

- **`class DryRunFileEntry(path: 'str', size: 'int', skipped_reason: 'str' = '') -> None`**
  One file the dry run encountered. Captured for the
largest-N + skipped lists; omitted from the count of in-scope
files (which is just an integer).

- **`class DryRunSummary(sources: 'List[str]' = <factory>, files_in_scope: 'int' = 0, bytes_in_scope: 'int' = 0, dirs_walked: 'int' = 0, files_skipped_size: 'int' = 0, files_skipped_excluded: 'int' = 0, files_unreadable: 'int' = 0, largest_in_scope: 'List[DryRunFileEntry]' = <factory>, extensions: 'Counter' = <factory>, elapsed_sec: 'float' = 0.0) -> None`**
  What a real backup would do, without doing it.

- **`def dry_run_plan(plan, *, exclusions=None) -> 'DryRunSummary'`**
  Convenience: dry-run every source in a Plan as a single
aggregated summary.

- **`def dry_run_source(source: 'Path', *, exclusions=None, max_file_bytes: 'Optional[int]' = None) -> 'DryRunSummary'`**
  Walk ``source`` + return what would be backed up.

### `arq_writer.exclusions`

Source-tree exclusion rules.

- **`class ExclusionRules(wildcard: 'Tuple[str, ...]' = (), regex: 'Tuple[str, ...]' = (), gitignore_lines: 'Tuple[str, ...]' = (), _compiled_regex: 'Tuple' = (), _gitignore_rules: 'Tuple' = ()) -> None`**
  Bundle of exclusion patterns. All fields default to empty.

### `arq_writer.json_configs`

Builders for the four JSON config files at the root of a backup set.

- **`def build_backupconfig(*, backup_name: 'str', computer_name: 'str', is_encrypted: 'bool' = True, blob_id_type: 'int' = 2, max_packed_item_length: 'int' = 256000, chunker_version: 'int' = 3) -> 'dict'`**
  Build ``backupconfig.json``.

- **`def build_backupfolder_json(*, folder_uuid: 'str', name: 'str', local_path: 'str', local_mount_point: 'str' = '/', storage_class: 'str' = 'STANDARD', disk_identifier: 'str' = 'ROOT') -> 'dict'`**
  Build per-folder ``backupfolder.json``.

- **`def build_backupfolders_json(computer_uuid: 'str') -> 'dict'`**
  Build the top-level ``backupfolders.json``.

- **`def build_backupplan(*, plan_uuid: 'str', plan_name: 'str', folder_plans: 'Iterable[dict]', is_encrypted: 'bool' = True, update_time: 'float' = 0.0, creation_time: 'float' = 0.0, storage_location_id: 'int' = 1) -> 'dict'`**
  Build ``backupplan.json``.

- **`def build_folder_plan(*, folder_uuid: 'str', local_path: 'str', name: 'str', local_mount_point: 'str' = '/', relative_path: 'str' = '/', disk_identifier: 'str' = 'ROOT') -> 'dict'`**
  Per-folder entry inside ``backupPlanJSON.backupFolderPlansByUUID``.

### `arq_writer.lz4_block`

Pure-Python LZ4 block-format codec.

- **`def lz4_block_compress(data: 'bytes') -> 'bytes'`**
  Emit ``data`` as a single literal sequence (no back-references).

- **`def lz4_block_decompress(block: 'bytes', expected_size: 'int' = -1) -> 'bytes'`**
  Decode an LZ4 block back to plaintext.

- **`def lz4_unwrap(wrapped: 'bytes') -> 'bytes'`**
  Reverse ``lz4_wrap``.

- **`def lz4_wrap(data: 'bytes') -> 'bytes'`**
  Apply Arq's outer wrapper: 4-byte BE length then LZ4 block.

### `arq_writer.macho_buzhash_finder`

Static analyzer for Mach-O binaries — locate Buzhash chunker
parameters in Arq.app (or any other macOS app that uses Buzhash).

- **`class InferredParameters(sample_count: 'int', min_chunk_observed: 'int', max_chunk_observed: 'int', median_chunk: 'int', mean_chunk: 'int', estimated_min_chunk_size: 'int', estimated_max_chunk_size: 'int', estimated_boundary_bits: 'int', confidence: 'str') -> None`**
  InferredParameters(sample_count: 'int', min_chunk_observed: 'int', max_chunk_observed: 'int', median_chunk: 'int', mean_chunk: 'int', estimated_min_chunk_size: 'int', estimated_max_chunk_size: 'int', estimated_boundary_bits: 'int', confidence: 'str')

- **`class MachOBuzhashReport(file_path: 'Path', file_size: 'int', is_macho: 'bool', t_table_candidates: 'List[TableCandidate]' = <factory>, numeric_constants: 'List[NumericHit]' = <factory>, notes: 'List[str]' = <factory>) -> None`**
  MachOBuzhashReport(file_path: 'Path', file_size: 'int', is_macho: 'bool', t_table_candidates: 'List[TableCandidate]' = <factory>, numeric_constants: 'List[NumericHit]' = <factory>, notes: 'List[str]' = <factory>)

- **`class MinMaxPair(min_value: 'int', max_value: 'int', min_offset: 'int', max_offset: 'int', distance: 'int') -> None`**
  A co-located (min, max) candidate pair.

- **`class NumericHit(offset: 'int', width: 'int', value: 'int', label: 'str') -> None`**
  A numeric constant matched somewhere in the binary.

- **`class TableCandidate(offset: 'int', score: 'float', unique_values: 'int', entropy_bits: 'float') -> None`**
  A 1024-byte window scored as a possible 256×4 Buzhash table.

- **`def analyze_macho_for_buzhash(path: 'Path', *, table_search_stride: 'int' = 16, top_k: 'int' = 10, widths: 'Tuple[int, ...]' = (4, 8)) -> 'MachOBuzhashReport'`**
  Run the full T-table + constants scan.

- **`def filter_constants_near(constants: 'List[NumericHit]', center_offset: 'int', *, radius_bytes: 'int' = 32768) -> 'List[NumericHit]'`**
  Return only the constants whose offset lies within
``±radius_bytes`` of ``center_offset`` (typically the top T-table
candidate's offset).

- **`def find_min_max_pairs(constants: 'List[NumericHit]', *, max_distance_bytes: 'int' = 64) -> 'List[MinMaxPair]'`**
  From a list of numeric-constant hits, find every (min, max)
pair whose offsets are within ``max_distance_bytes`` of each
other, in plausibility order.

- **`def find_numeric_constants(data: 'bytes', *, widths: 'Tuple[int, ...]' = (4, 8)) -> 'List[NumericHit]'`**
  Locate every plausible chunker constant value.

- **`def find_t_table_candidates(data: 'bytes', *, top_k: 'int' = 10, stride: 'int' = 4) -> 'List[TableCandidate]'`**
  Slide a 1024-byte window over ``data`` (4-byte aligned) and
return the top-K scored windows.

- **`def infer_parameters_from_chunk_sizes(chunk_sizes: 'List[int]') -> 'InferredParameters'`**
  Given the lengths of the chunks Arq.app produced for one or
more files, estimate Buzhash parameters.

### `arq_writer.macos_snapshot`

macOS APFS snapshot-based backup source helper.

- **`class NotMacOSError(...)`**
  Raised when an APFS-snapshot operation is attempted off macOS.

- **`class SnapshotError(...)`**
  Raised when an `tmutil` / `mount_apfs` command fails.

- **`class SnapshotInfo(name: 'str', creation_iso: 'str' = '') -> None`**
  One APFS local snapshot as listed by ``tmutil
listlocalsnapshots``.

- **`def create_snapshot() -> 'SnapshotInfo'`**
  Run ``sudo tmutil localsnapshot`` and return the new snapshot.

- **`def delete_snapshot(snapshot: 'SnapshotInfo') -> 'None'`**
  ``sudo tmutil deletelocalsnapshots <date>``.

- **`def is_macos() -> 'bool'`**

- **`def is_macos_apfs(path: 'Path') -> 'bool'`**
  Return True iff ``path`` lives on a macOS APFS volume.

- **`def list_snapshots(volume: 'str' = '/') -> 'List[SnapshotInfo]'`**
  ``tmutil listlocalsnapshots <volume>`` parsed into entries.

- **`def mount_snapshot(snapshot: 'SnapshotInfo', *, mount_point: 'Path', device: 'str' = '/System/Volumes/Data') -> 'Path'`**
  ``sudo mount_apfs -s <snap> <device> <mount_point>``.

- **`def unmount_snapshot(mount_point: 'Path') -> 'None'`**
  ``sudo umount <mount_point>``. Idempotent — already-
unmounted paths are silently OK.

- **`def with_apfs_snapshot(source: 'Path', *, delete_on_exit: 'bool' = False, mount_root: 'Optional[Path]' = None) -> 'Iterator[Path]'`**
  Context manager: yield a path inside an APFS snapshot whose
contents match ``source`` at entry time.

### `arq_writer.pack_builder`

Arq 7 pack file emitter.

- **`class PackBuilder(computer_uuid: 'str', family: 'str', dest_root: 'Path', *, max_pack_bytes: 'int' = 10485760, compression_type: 'int' = 2, backend=None) -> 'None'`**
  Per-family in-memory buffer that flushes ``.pack`` files on
threshold.

- **`class PackFileInfo(relative_path: 'str', size: 'int', blob_count: 'int', blob_ids: 'List[str]' = <factory>) -> None`**
  One emitted ``.pack`` file.

### `arq_writer.prior_tree`

Lazy index over a prior backup's tree, keyed by source-relative path.

- **`class PriorMatch(node: 'FileNode', matches: 'bool') -> None`**
  Result of comparing a source ``stat`` to a prior FileNode.

- **`class PriorTreeIndex(dest_root: 'Path', computer_uuid: 'str', encryption_key: 'bytes', hmac_key: 'bytes', *, folder_uuid: 'Optional[str]' = None, openssl_path: 'str' = 'openssl', backend=None, max_cache_trees: 'Optional[int]' = None) -> 'None'`**
  Lazy, path-keyed view over a prior backup's tree.

- **`def reuse_file_node_for(src_stat: 'os.stat_result', prior: 'FileNode') -> 'FileNode'`**
  Build a FileNode that shares the prior blob locations but
reflects a fresh ``stat`` snapshot of the source — preserves
inode-level metadata changes (uid/gid/nlink) without re-reading
file contents.

### `arq_writer.retention`

Retention policy + backuprecord pruning + blob garbage collection.

- **`class GcResult(standalone_blobs_deleted: 'int' = 0, standalone_bytes_freed: 'int' = 0, packs_deleted: 'int' = 0, pack_bytes_freed: 'int' = 0, packs_partially_orphan_kept: 'int' = 0, failures: 'List[Dict[str, str]]' = <factory>, dry_run: 'bool' = False) -> None`**
  GcResult(standalone_blobs_deleted: 'int' = 0, standalone_bytes_freed: 'int' = 0, packs_deleted: 'int' = 0, pack_bytes_freed: 'int' = 0, packs_partially_orphan_kept: 'int' = 0, failures: 'List[Dict[str, str]]' = <factory>, dry_run: 'bool' = False)

- **`class PruneRecordsResult(deleted: 'List[str]' = <factory>, retained: 'List[str]' = <factory>, failures: 'List[Dict[str, str]]' = <factory>) -> None`**
  PruneRecordsResult(deleted: 'List[str]' = <factory>, retained: 'List[str]' = <factory>, failures: 'List[Dict[str, str]]' = <factory>)

- **`class RetentionPolicy(keep_last_n: 'Optional[int]' = None, keep_hourly: 'int' = 0, keep_daily: 'int' = 0, keep_weekly: 'int' = 0, keep_monthly: 'int' = 0, keep_yearly: 'int' = 0) -> None`**
  Multi-bucket retention.

- **`class RetentionResult(prune: 'PruneRecordsResult', gc: 'Optional[GcResult]' = None) -> None`**
  RetentionResult(prune: 'PruneRecordsResult', gc: 'Optional[GcResult]' = None)

- **`def apply_retention(backend: 'Backend', *, encryption_password: 'str', policy: 'RetentionPolicy', computer_uuid: 'Optional[str]' = None, root: 'str' = '/', folder_uuid: 'Optional[str]' = None, run_gc: 'bool' = True, dry_run: 'bool' = False, openssl_path: 'str' = 'openssl', callback=None) -> 'RetentionResult'`**
  Compose record-pruning and blob GC in the correct order.

- **`def gc_orphan_blobs(backend: 'Backend', *, encryption_password: 'str', computer_uuid: 'Optional[str]' = None, root: 'str' = '/', openssl_path: 'str' = 'openssl', dry_run: 'bool' = False, callback=None) -> 'GcResult'`**
  Conservative GC: delete standalone objects whose blob_id
isn't referenced by any surviving record, plus pack files
where **every** referenced blob is unreferenced (i.e. no live
record points into the pack).

- **`def prune_records(backend: 'Backend', *, encryption_password: 'str', computer_uuid: 'Optional[str]' = None, policy: 'RetentionPolicy', root: 'str' = '/', folder_uuid: 'Optional[str]' = None, openssl_path: 'str' = 'openssl', dry_run: 'bool' = False, callback=None) -> 'PruneRecordsResult'`**
  Apply ``policy`` per folder; delete records that fall
outside retention.

- **`def select_retained(records: 'List[_RecordRef]', policy: 'RetentionPolicy') -> 'Set[str]'`**
  Apply ``policy`` to ``records`` and return the set of paths
that survive. ``records`` must be **sorted oldest-first** for
"newest in each bucket" semantics.

### `arq_writer.serialize`

Binary serializers for Arq 7's ``Node`` / ``Tree`` / ``BlobLoc``.

- **`def write_blobloc(loc: 'BlobLoc') -> 'bytes'`**
  Serialize one BlobLoc in Arq 7's on-disk binary layout.

- **`def write_bool(value: 'bool') -> 'bytes'`**

- **`def write_int32(value: 'int') -> 'bytes'`**

- **`def write_int64(value: 'int') -> 'bytes'`**

- **`def write_node(node: 'Node', *, tree_version: 'int' = 3) -> 'bytes'`**
  Serialize a ``Node`` (file or directory).

- **`def write_string(value: 'Optional[str]') -> 'bytes'`**
  Encode an optional UTF-8 string per Arq's [String] convention.

- **`def write_tree(tree: 'Tree', *, version: 'int' = 3) -> 'bytes'`**
  Serialize a ``Tree`` (directory metadata + child Nodes by name).

- **`def write_uint32(value: 'int') -> 'bytes'`**

- **`def write_uint64(value: 'int') -> 'bytes'`**

### `arq_writer.types`

Dataclasses mirroring Arq 7's ``Node`` / ``Tree`` / ``BlobLoc`` types.

- **`class BlobLoc(blobIdentifier: 'str', isPacked: 'bool' = False, isLargePack: 'bool' = False, relativePath: 'str' = '', offset: 'int' = 0, length: 'int' = 0, stretchEncryptionKey: 'bool' = True, compressionType: 'int' = 2) -> None`**
  Pointer to a blob's bytes inside a pack file or standalone object.

- **`class FileNode(dataBlobLocs: 'List[BlobLoc]' = <factory>, itemSize: 'int' = 0, containedFilesCount: 'int' = 1, mtime_sec: 'int' = 0, mtime_nsec: 'int' = 0, ctime_sec: 'int' = 0, ctime_nsec: 'int' = 0, create_time_sec: 'int' = 0, create_time_nsec: 'int' = 0, username: 'Optional[str]' = None, groupName: 'Optional[str]' = None, deleted: 'bool' = False, mac_st_dev: 'int' = 0, mac_st_ino: 'int' = 0, mac_st_mode: 'int' = 0, mac_st_nlink: 'int' = 1, mac_st_uid: 'int' = 0, mac_st_gid: 'int' = 0, mac_st_rdev: 'int' = 0, mac_st_flags: 'int' = 0, win_attrs: 'int' = 0, win_reparse_tag: 'int' = 0, win_reparse_point_is_directory: 'bool' = False, computerOSType: 'int' = 1, aclBlobLoc: 'Optional[BlobLoc]' = None, xattrsBlobLocs: 'List[BlobLoc]' = <factory>) -> None`**
  ``Node`` describing a regular file (``isTree = false``).

- **`class Tree(children: "List['TreeChild']" = <factory>, version: 'int' = 0) -> None`**
  ``Tree`` — directory metadata + child Nodes by name.

- **`class TreeChild(name: 'str', node: 'Node') -> None`**
  TreeChild(name: 'str', node: 'Node')

- **`class TreeNode(treeBlobLoc: 'BlobLoc', itemSize: 'int' = 0, containedFilesCount: 'int' = 0, mtime_sec: 'int' = 0, mtime_nsec: 'int' = 0, ctime_sec: 'int' = 0, ctime_nsec: 'int' = 0, create_time_sec: 'int' = 0, create_time_nsec: 'int' = 0, username: 'Optional[str]' = None, groupName: 'Optional[str]' = None, deleted: 'bool' = False, mac_st_dev: 'int' = 0, mac_st_ino: 'int' = 0, mac_st_mode: 'int' = 16877, mac_st_nlink: 'int' = 1, mac_st_uid: 'int' = 0, mac_st_gid: 'int' = 0, mac_st_rdev: 'int' = 0, mac_st_flags: 'int' = 0, win_attrs: 'int' = 0, win_reparse_tag: 'int' = 0, win_reparse_point_is_directory: 'bool' = False, computerOSType: 'int' = 1, aclBlobLoc: 'Optional[BlobLoc]' = None, xattrsBlobLocs: 'List[BlobLoc]' = <factory>) -> None`**
  ``Node`` describing a directory (``isTree = true``).

### `arq_writer.xattrs`

Extended-attribute capture + restore.

- **`def apply_xattrs(path: 'os.PathLike', xattrs: 'Dict[str, bytes]', *, callback: 'Optional[Callable[..., None]]' = None) -> 'int'`**
  Write each ``name → value`` to ``path`` via ``os.setxattr``.

- **`def capture_xattrs(path: 'os.PathLike', *, callback: 'Optional[Callable[..., None]]' = None) -> 'Dict[str, bytes]'`**
  Read every extended attribute on ``path`` and return a
name → value dict.

- **`def deserialize_xattrs(blob: 'bytes') -> 'Dict[str, bytes]'`**
  Decode an xattr blob.

- **`def has_xattr_support() -> 'bool'`**
  Return True iff the current host exposes xattr APIs.

- **`def serialize_xattrs(xattrs: 'Dict[str, bytes]') -> 'bytes'`**
  Encode a name → value dict as Arq.app's ``XAttrSetV002`` blob.

## `arq_reader`

### `arq_reader.arq5_binary`

Arq 5 / Arq 6 ``Tree`` / ``Node`` / ``Commit`` / ``BlobKey`` parsers.

- **`class Arq5Commit(version: 'int', author: 'Optional[str]' = None, comment: 'Optional[str]' = None, parentCommitBlobKey: 'Optional[BlobKey]' = None, treeBlobKey: 'Optional[BlobKey]' = None, location: 'Optional[str]' = None, creationDate: 'Optional[datetime]' = None, failedFiles: 'List[Dict[str, Optional[str]]]' = <factory>, hasMissingNodes: 'bool' = False, isComplete: 'bool' = True, bucketXmlData: 'bytes' = b'', arqVersion: 'Optional[str]' = None) -> None`**
  Decoded Arq 5/6 Commit.

- **`class Arq5Node(isTree: 'bool' = False, treeContainsMissingItems: 'bool' = False, dataCompressionType: 'int' = 0, xattrsCompressionType: 'int' = 0, aclCompressionType: 'int' = 0, dataBlobKeys: 'List[BlobKey]' = <factory>, uncompressedDataSize: 'int' = 0, xattrsBlobKey: 'Optional[BlobKey]' = None, xattrsSize: 'int' = 0, aclBlobKey: 'Optional[BlobKey]' = None, uid: 'int' = 0, gid: 'int' = 0, mode: 'int' = 0, mtime_sec: 'int' = 0, mtime_nsec: 'int' = 0, flags: 'int' = 0, finderFlags: 'int' = 0, extendedFinderFlags: 'int' = 0, finderFileType: 'Optional[str]' = None, finderFileCreator: 'Optional[str]' = None, isFileExtensionHidden: 'bool' = False, st_dev: 'int' = 0, st_ino: 'int' = 0, st_nlink: 'int' = 0, st_rdev: 'int' = 0, ctime_sec: 'int' = 0, ctime_nsec: 'int' = 0, createTime_sec: 'int' = 0, createTime_nsec: 'int' = 0, st_blocks: 'int' = 0, st_blksize: 'int' = 0) -> None`**
  One Arq 5/6 file or directory entry inside a Tree.

- **`class Arq5ParseError(...)`**
  Raised when the input doesn't match the Arq 5 spec.

- **`class Arq5Tree(version: 'int', xattrsCompressionType: 'int' = 0, aclCompressionType: 'int' = 0, xattrsBlobKey: 'Optional[BlobKey]' = None, xattrsSize: 'int' = 0, aclBlobKey: 'Optional[BlobKey]' = None, uid: 'int' = 0, gid: 'int' = 0, mode: 'int' = 0, mtime_sec: 'int' = 0, mtime_nsec: 'int' = 0, flags: 'int' = 0, finderFlags: 'int' = 0, extendedFinderFlags: 'int' = 0, st_dev: 'int' = 0, st_ino: 'int' = 0, st_nlink: 'int' = 0, st_rdev: 'int' = 0, ctime_sec: 'int' = 0, ctime_nsec: 'int' = 0, st_blocks: 'int' = 0, st_blksize: 'int' = 0, createTime_sec: 'int' = 0, createTime_nsec: 'int' = 0, missing_nodes: 'Dict[str, Arq5Node]' = <factory>, nodes: 'Dict[str, Arq5Node]' = <factory>) -> None`**
  Decoded Arq 5/6 Tree.

- **`class BinaryStream(data: 'bytes', pos: 'int' = 0) -> 'None'`**
  Stateful byte reader for Arq 5/6 binary records.

- **`class BlobKey(sha1: 'str', stretchEncryptionKey: 'bool' = False, storageType: 'int' = 1, archiveId: 'Optional[str]' = None, archiveSize: 'int' = 0, archiveUploadedDate: 'Optional[datetime]' = None, compressionType: 'int' = 0) -> None`**
  An Arq 5/6 blob reference.

- **`def parse_blobkey(s: 'BinaryStream', tree_version: 'int', *, compression_type: 'int' = 0) -> 'Optional[BlobKey]'`**
  Parse a ``BlobKey`` from ``s``.

- **`def parse_commit(data: 'bytes') -> 'Arq5Commit'`**
  Parse an Arq 5/6 Commit blob plaintext.

- **`def parse_node(s: 'BinaryStream', tree_version: 'int') -> 'Arq5Node'`**
  Parse one Node inside a Tree of version ``tree_version``.

- **`def parse_tree(data: 'bytes') -> 'Arq5Tree'`**
  Parse an Arq 5/6 Tree (the LZ4-/Gzip-decompressed payload of
a tree blob, minus the EncryptedObject envelope).

### `arq_reader.arq5_keyset`

Arq 5 / Arq 6 ``encryptionvN.dat`` master-key file decryption.

- **`class Arq5Keyset(encryption_version: 'int', encryption_key: 'bytes', hmac_key: 'bytes', blob_id_salt: 'bytes') -> None`**
  Decrypted Arq 5/6 master keys.

- **`def arq5_compute_blob_sha1(plaintext: 'bytes', blob_id_salt: 'bytes') -> 'str'`**
  Arq 5/6 blob ID = SHA-1(salt + plaintext), hex.

- **`def arq5_object_paths(computer_uuid: 'str', sha1: 'str') -> 'Tuple[str, ...]'`**
  Return the candidate paths to try, in order, for a given SHA-1.

- **`def build_arq5_keyset_blob(password: 'str', encryption_key: 'bytes', hmac_key: 'bytes', blob_id_salt_for_v3: 'Optional[bytes]' = None, *, salt: 'Optional[bytes]' = None, iv: 'Optional[bytes]' = None, openssl_path: 'str' = 'openssl') -> 'bytes'`**
  Build a fresh ``encryptionv2.dat`` (no salt arg) or
``encryptionv3.dat`` (salt arg present) blob.

- **`def decrypt_arq5_keyset(data: 'bytes', password: 'str', computer_uuid: 'str', *, openssl_path: 'str' = 'openssl') -> 'Arq5Keyset'`**
  Decrypt an Arq 5/6 ``encryptionv2.dat`` or ``encryptionv3.dat``
blob and return the master keys.

### `arq_reader.arq5_pack`

Arq 5 / Arq 6 ``.pack`` and ``.index`` file parsers.

- **`class PackEntry(mimetype: 'str', download_name: 'str', data: 'bytes') -> None`**
  One entry inside an Arq 5/6 ``.pack`` file.

- **`class PackIndexEntry(offset: 'int', data_length: 'int', sha1_hex: 'str') -> None`**
  PackIndexEntry(offset: 'int', data_length: 'int', sha1_hex: 'str')

- **`def build_pack_file(entries: 'List[Tuple[str, int, bytes]]') -> 'bytes'`**
  Build an Arq 5/6 ``.pack`` file body.

- **`def build_pack_index(entries: 'List[Tuple[str, int, int]]') -> 'bytes'`**
  Build an Arq 5/6 ``.index`` file body.

- **`def parse_pack_file(data: 'bytes') -> 'List[PackEntry]'`**
  Parse an Arq 5/6 ``.pack`` file into entries.

- **`def parse_pack_index(data: 'bytes') -> 'List[PackIndexEntry]'`**
  Parse an Arq 5/6 ``.index`` file body into entries.

### `arq_reader.arq5_restore`

High-level Arq 5 / Arq 6 restorer.

- **`class Arq5Restore(src: 'Path', encryption_password: 'str', *, openssl_path: 'str' = 'openssl') -> 'None'`**
  One-shot restore against an Arq 5/6 backup destination.

- **`class Arq5RestoreResult(src: 'Path', dest: 'Path', computer_uuid: 'str', folder_uuid: 'str', commit_sha1: 'str' = '', files_restored: 'int' = 0, dirs_restored: 'int' = 0, bytes_restored: 'int' = 0, blobs_fetched: 'int' = 0, failures: 'List[Dict[str, str]]' = <factory>) -> None`**
  Arq5RestoreResult(src: 'Path', dest: 'Path', computer_uuid: 'str', folder_uuid: 'str', commit_sha1: 'str' = '', files_restored: 'int' = 0, dirs_restored: 'int' = 0, bytes_restored: 'int' = 0, blobs_fetched: 'int' = 0, failures: 'List[Dict[str, str]]' = <factory>)

### `arq_reader.cli`

Standalone CLI: ``python -m arq_reader {list,restore} ...``.

- **`def main(argv: 'Optional[List[str]]' = None) -> 'int'`**

### `arq_reader.decrypt`

Full ``EncryptedObject`` decryption.

- **`class DecryptError(...)`**
  Raised on HMAC mismatch, malformed ARQO, or AES failure.

- **`def decrypt_encrypted_object(arqo: 'bytes', encryption_key: 'bytes', hmac_key: 'bytes', *, openssl_path: 'str' = 'openssl', skip_hmac: 'bool' = False) -> 'bytes'`**
  Decrypt a single ``EncryptedObject`` blob to its raw plaintext.

- **`def decrypt_lz4_arqo(arqo: 'bytes', encryption_key: 'bytes', hmac_key: 'bytes', *, openssl_path: 'str' = 'openssl', skip_hmac: 'bool' = False) -> 'bytes'`**
  Decrypt then LZ4-unwrap.

### `arq_reader.pack`

Arq 7 pack file walker + index reconstructor.

- **`class PackEntry(offset: 'int', length: 'int', truncated: 'bool' = False, blob_id: 'Optional[str]' = None) -> None`**
  One ARQO blob recovered from a pack file.

- **`class PackError(...)`**
  Raised on malformed pack data.

- **`class PackSummary(total_size: 'int', entry_count: 'int', payload_bytes: 'int', truncated_tail: 'bool') -> None`**
  Lightweight stats from :func:`pack_summary`. ``total_size``
is the byte length of the pack as supplied; ``entry_count``
counts both intact and truncated entries; ``truncated_tail``
flips when the very last entry is incomplete.

- **`class PackTruncated(...)`**
  Final entry in the pack was truncated mid-ciphertext.

- **`def decode_pack(pack_bytes: 'bytes', encryption_key: 'bytes', hmac_key: 'bytes', *, openssl_path: 'str' = 'openssl', skip_truncated: 'bool' = True) -> 'Iterator[Tuple[PackEntry, bytes]]'`**
  Yield ``(entry, plaintext)`` for every blob in the pack.

- **`def pack_summary(pack_bytes: 'bytes') -> 'PackSummary'`**
  Inspect a pack file without keys, returning quick stats.

- **`def reconstruct_index(pack_bytes: 'bytes') -> 'List[PackEntry]'`**
  Walk a pack file from offset 0 and return every entry's
``(offset, length)``.

### `arq_reader.parse`

Binary parsers for Arq 7's ``Node`` / ``Tree`` / ``BlobLoc``.

- **`class BinaryReader(data: 'bytes', pos: 'int' = 0) -> 'None'`**
  Tiny stateful reader over a bytes buffer.

- **`def parse_blobloc(reader: 'BinaryReader') -> 'BlobLoc'`**
  Parse one binary BlobLoc entry.

- **`def parse_node(reader: 'BinaryReader', *, tree_version: 'int')`**
  Parse a Node (file or directory).

- **`def parse_tree(data: 'bytes') -> 'Tree'`**
  Parse a Tree's binary form (typically returned by
``decrypt_lz4_arqo`` for a treeBlobLoc target).

### `arq_reader.restore`

High-level restore orchestrator.

- **`class DryRunRestoreResult(src: 'Path', folder_uuid: 'str', backuprecord_path: 'str' = '', files_listed: 'int' = 0, dirs_listed: 'int' = 0, bytes_would_restore: 'int' = 0, sample_paths: 'List[str]' = <factory>) -> None`**
  Output of :meth:`Restore.dry_run_restore`.

- **`class RecordInfo(computer_uuid: 'str', folder_uuid: 'str', relative_path: 'str', creation_date: 'int' = 0, arq_version: 'str' = '', is_complete: 'bool' = True) -> None`**
  Summary of one backuprecord, suitable for record-history UIs.

- **`class Restore(src: 'Path', encryption_password: 'str', *, openssl_path: 'str' = 'openssl', backend: 'Optional[Backend]' = None, on_conflict: 'str' = 'overwrite') -> 'None'`**
  Stateful restorer.

- **`class RestoreResult(src: 'Path', dest: 'Path', folder_uuid: 'str', files_restored: 'int' = 0, dirs_restored: 'int' = 0, bytes_restored: 'int' = 0, blobs_fetched: 'int' = 0, failures: 'List[Dict[str, str]]' = <factory>) -> None`**
  RestoreResult(src: 'Path', dest: 'Path', folder_uuid: 'str', files_restored: 'int' = 0, dirs_restored: 'int' = 0, bytes_restored: 'int' = 0, blobs_fetched: 'int' = 0, failures: 'List[Dict[str, str]]' = <factory>)

### `arq_reader.restore_verify`

Post-restore integrity verification.

- **`class VerifyFailure(path: 'str', kind: 'str', expected: 'str' = '', actual: 'str' = '') -> None`**
  One file whose restored bytes don't match the recorded
blob_id.

- **`class VerifyReport(files_verified: 'int' = 0, files_skipped_chunked: 'int' = 0, files_missing: 'int' = 0, failures: 'List[VerifyFailure]' = <factory>) -> None`**
  Aggregate result of :func:`verify_restored`.

- **`def verify_restored(restored_root: 'Path', record: 'Dict[str, Any]', *, blob_id_salt: 'bytes' = b'') -> 'VerifyReport'`**
  Walk the source-recorded tree + check each restored file's
bytes match the recorded blob_id chain.

- **`def verify_restored_walk(restored_root: 'Path', *, expected_size_total: 'Optional[int]' = None, expected_file_count: 'Optional[int]' = None) -> 'VerifyReport'`**
  Lightweight walk-only verifier. Counts files + total bytes
actually restored + compares to the supplied expected totals.

### `arq_reader.snapshot_diff`

Compare two backuprecords (snapshots) to surface what changed
between them.

- **`class DedupReport(snapshot_a: 'str' = '', snapshot_b: 'str' = '', a_blob_count: 'int' = 0, b_blob_count: 'int' = 0, shared_blob_count: 'int' = 0, a_unique_count: 'int' = 0, b_unique_count: 'int' = 0, a_blob_bytes: 'int' = 0, b_blob_bytes: 'int' = 0, shared_blob_bytes: 'int' = 0) -> None`**
  Output of :func:`measure_dedup_ratio`.

- **`class SnapshotDiffEntry(kind: 'str', rel_path: 'str', a_size: 'Optional[int]' = None, b_size: 'Optional[int]' = None, a_mtime_sec: 'Optional[int]' = None, b_mtime_sec: 'Optional[int]' = None) -> None`**
  One change between two snapshots.

- **`class SnapshotDiffResult(entries: 'List[SnapshotDiffEntry]' = <factory>, counts: 'Dict[str, int]' = <factory>) -> None`**
  Aggregate result of :func:`diff_snapshots`. Operators
typically just iterate over ``entries``; the tally counts are
convenient for headline reporting.

- **`def diff_snapshots(restore, *, record_path_a: 'str', record_path_b: 'str', computer_uuid: 'Optional[str]' = None) -> 'SnapshotDiffResult'`**
  Walk both backuprecords' trees and report every difference.

- **`def measure_dedup_ratio(restore, *, record_path_a: 'str', record_path_b: 'str', computer_uuid: 'Optional[str]' = None) -> 'DedupReport'`**
  Walk both snapshots' trees + report the shared/unique
blob breakdown.

## `arq_validator`

### `arq_validator.audit_drip`

Resumable audit-drip orchestrator.

- **`class AuditDripState(target: 'str' = '', sweep_started_at: 'float' = 0.0, sweep_completed_at: 'Optional[float]' = None, sweep_count: 'int' = 0, cursor_computer: 'Optional[str]' = None, cursor_kind: 'Optional[str]' = None, cursor_shard: 'Optional[str]' = None, cursor_file_name: 'Optional[str]' = None, files_audited_this_sweep: 'int' = 0, files_total_this_sweep: 'int' = 0, bytes_audited_this_sweep: 'int' = 0, inner_arqos_audited_this_sweep: 'int' = 0, inner_arqos_ok_this_sweep: 'int' = 0, inner_arqos_fail_this_sweep: 'int' = 0, fails_this_sweep: 'int' = 0, errors_this_sweep: 'int' = 0, skipped_this_sweep: 'int' = 0, failed_files_this_sweep: 'List[Dict[str, str]]' = <factory>, last_fire_started_at: 'float' = 0.0, last_fire_finished_at: 'float' = 0.0, last_fire_elapsed_sec: 'float' = 0.0, last_fire_files_processed: 'int' = 0, last_fire_aborted_reason: 'Optional[str]' = None, last_fire_keyset_decrypted: 'Optional[bool]' = None, paused_until_epoch: 'Optional[float]' = None, error: 'Optional[str]' = None) -> None`**
  Persistent cursor + per-sweep accumulators for audit-drip.

- **`class Throttle(files_per_min: 'Optional[float]') -> 'None'`**
  Minimum inter-event spacing.

- **`def build_walk(layouts: 'List[L.Arq7ComputerLayout]') -> 'List[Tuple[str, str, str, str]]'`**
  Deterministic walk: ``(computer, family, shard, file_name)``.

- **`def load_state(state_file: 'Path', target: 'str') -> 'AuditDripState'`**
  Load existing state or return a fresh ``AuditDripState``.

- **`def pause(state_file: 'Path', target: 'str', *, until_epoch: 'float') -> 'AuditDripState'`**
  Set ``paused_until_epoch`` so subsequent fires silent-skip.

- **`def resume(state_file: 'Path', target: 'str') -> 'AuditDripState'`**
  Clear any pause so the next fire runs.

- **`def run_audit_drip(backend: 'Backend', *, target: 'str', state_file: 'Path', encryption_password: 'str', root: 'str' = '/', max_runtime_sec: 'int' = 0, rate_files_per_min: 'Optional[float]' = None, skip_larger_than: 'Optional[int]' = 262144, discover_concurrency: 'int' = 8, failed_list_cap: 'int' = 100, openssl_path: 'str' = 'openssl', callback: 'Optional[ProgressCallback]' = None) -> 'AuditDripState'`**
  Run one fire of the resumable L2 audit.

- **`def save_state(state: 'AuditDripState', state_file: 'Path') -> 'None'`**

### `arq_validator.backend`

Storage backend abstraction.

- **`class Backend(*args, **kwargs)`**
  Minimal interface a storage backend must implement.

- **`class LocalBackend(root: 'os.PathLike') -> 'None'`**
  Local filesystem backend rooted at a directory.

### `arq_validator.cli`

Standalone CLI for the Arq validator.

- **`def main(argv: 'Optional[List[str]]' = None) -> 'int'`**

### `arq_validator.compatibility`

Arq 7 on-disk format compatibility checker.

- **`class CheckResult(id: 'str', name: 'str', passed: 'bool', message: 'str' = '', details: 'Dict[str, Any]' = <factory>) -> None`**
  One pass/fail entry in a :class:`ComplianceReport`.

- **`class ComplianceReport(destination_root: 'str', computer_uuid: 'str', folder_uuids: 'List[str]' = <factory>, checks: 'List[CheckResult]' = <factory>) -> None`**
  Aggregate report from :func:`check_arq7_compatibility`.

- **`def check_arq7_compatibility(backend: 'Backend', root: 'str' = '/', *, encryption_password: 'str', computer_uuid: 'Optional[str]' = None, openssl_path: 'str' = 'openssl') -> 'ComplianceReport'`**
  Run every Arq 7 invariant against the destination at
``root`` inside ``backend``.

### `arq_validator.crypto`

Crypto primitives for Arq 7 keyset decryption and HMAC verification.

- **`class CryptoError(...)`**
  Wrong password, corrupted keyset, missing openssl, etc.

- **`class Keyset(encryption_key: 'bytes', hmac_key: 'bytes', blob_id_salt: 'bytes') -> None`**
  Decrypted master keys held in process memory.

- **`def aes_256_cbc_decrypt(key: 'bytes', iv: 'bytes', ciphertext: 'bytes', *, openssl_path: 'str' = 'openssl') -> 'bytes'`**
  Decrypt ``ciphertext`` via the openssl CLI.

- **`def decrypt_keyset(storage_data: 'bytes', password: 'str', *, openssl_path: 'str' = 'openssl') -> 'Keyset'`**
  Full ``encryptedkeyset.dat`` -> ``Keyset`` pipeline.

- **`def derive_keyset_keys(password: 'str', salt: 'bytes') -> 'Tuple[bytes, bytes]'`**
  PBKDF2-SHA256(password, salt) -> (aes_key[32], hmac_key[32]).

- **`def find_arqo_positions(body: 'bytes') -> 'list'`**
  Return offsets where ``b"ARQO"`` appears in ``body``.

- **`def parse_keyset_plaintext(data: 'bytes') -> 'Keyset'`**
  Parse the AES-decrypted keyset plaintext into a ``Keyset``.

- **`def parse_keyset_storage(data: 'bytes') -> 'Tuple[bytes, bytes, bytes, bytes]'`**
  Split encryptedkeyset.dat into (salt, hmac_stored, iv, ciphertext).

- **`def verify_encrypted_object_hmac(data: 'bytes', hmac_key: 'bytes') -> 'Tuple[bool, str, str]'`**
  Verify a single EncryptedObject's HMAC field.

- **`def verify_keyset_hmac(hmac_key: 'bytes', iv: 'bytes', ciphertext: 'bytes', expected: 'bytes') -> 'bool'`**
  HMAC-SHA256(hmac_key, IV + ciphertext) == expected?

- **`def verify_multi_object_arqos(body: 'bytes', hmac_key: 'bytes') -> 'Tuple[int, int, list]'`**
  HMAC-verify every inner ARQO in a multi-object pack body.

### `arq_validator.debug_logging`

Centralised debug-logging configuration.

- **`def enable_debug_logging(*, subsystems: 'Optional[List[str]]' = None, stream=None, level: 'int' = 10) -> 'None'`**
  Configure the arq_backup_tui logger family for verbose
output.

- **`def get_logger(subsystem: 'str' = '') -> 'logging.Logger'`**
  Return the named logger for ``subsystem``.

- **`def is_debug_enabled(subsystem: 'str' = '') -> 'bool'`**
  Operators / library code can short-circuit expensive
diagnostic-payload formatting by checking this first.

- **`def parse_debug_flag(value: 'str') -> 'List[str]'`**
  Convert a ``--debug`` value to the subsystem list.

### `arq_validator.events`

Progress events emitted by validation tiers.

- **`class Event(kind: 'EventKind', message: 'str' = '', payload: 'Dict[str, Any]' = <factory>) -> None`**
  Single progress event.

- **`class EventKind(*values)`**
  All progress event types emitted by validation tiers.

- **`def emit(callback: 'Optional[ProgressCallback]', kind: 'EventKind', message: 'str' = '', **payload: 'Any') -> 'None'`**
  Build and dispatch an ``Event``, swallowing callback exceptions.

- **`def noop_callback(event: 'Event') -> 'None'`**
  No-op callback for callers that don't want progress events.

### `arq_validator.fingerprint`

Shape fingerprint of an Arq 7 destination.

- **`class FileFingerprint(rel_path: 'str', item_size: 'int', mtime_sec: 'int', mode_perms: 'int', is_symlink: 'bool', chunk_sizes: 'List[int]' = <factory>) -> None`**
  Per-file shape entry. ``chunk_sizes`` is the in-order list
of plaintext lengths the chunker produced.

- **`def compute_shape_fingerprint(backend: 'Backend', *, encryption_password: 'str', root: 'str' = '/', computer_uuid: 'Optional[str]' = None, openssl_path: 'str' = 'openssl') -> 'Dict[str, Any]'`**
  Walk ``backend`` rooted at ``root`` and return a JSON-
serializable dict describing the destination's structural shape.

- **`def diff_fingerprints(a: 'Dict[str, Any]', b: 'Dict[str, Any]') -> 'Dict[str, Any]'`**
  Compare two fingerprints and return a structured diff.

### `arq_validator.fingerprint_cli`

CLI for shape-fingerprint generation + comparison.

- **`def main(argv: 'Optional[List[str]]' = None) -> 'int'`**

### `arq_validator.incremental_audit`

Persistent set of already-audited blob_ids so subsequent
validation runs can skip what they've already proven good.

- **`class AuditLedger(target: 'str' = '', schema_version: 'int' = 1, blob_last_ok: 'Dict[str, float]' = <factory>, sweep_count: 'int' = 0, last_sweep_finished_at: 'float' = 0.0) -> None`**
  Persistent set of audited blob_ids + per-id last-seen
epoch.

- **`def default_ledger_dir() -> 'Path'`**
  Operator-friendly default location, honoring XDG.

- **`def ledger_path_for(target: 'str') -> 'Path'`**
  Per-destination ledger file under the default dir.

- **`def load_ledger(path: 'Path', *, target: 'str' = '') -> 'AuditLedger'`**
  Load an existing ledger or return a fresh empty one.

- **`def merge_ledgers(a: 'AuditLedger', b: 'AuditLedger') -> 'AuditLedger'`**
  Combine two ledgers, keeping the later of the two
timestamps for each shared blob_id. Useful when running
audit on multiple machines against the same destination
+ then merging the ledgers manually.

- **`def prune_older_than(ledger: 'AuditLedger', age_sec: 'float', *, now: 'Optional[float]' = None) -> 'int'`**
  Drop ledger entries last confirmed more than
``age_sec`` seconds ago. Returns the count removed.

- **`def save_ledger(ledger: 'AuditLedger', path: 'Path') -> 'None'`**
  Atomic write — same write-tmp-then-rename pattern the
state-file IPC uses, so a crash mid-save can't leave a
half-written ledger.

### `arq_validator.layout`

Arq 7 directory-layout discovery.

- **`class Arq7ComputerLayout(computer_uuid: 'str', blobpacks: 'List[Tuple[str, str]]' = <factory>, treepacks: 'List[Tuple[str, str]]' = <factory>, largeblobpacks: 'List[Tuple[str, str]]' = <factory>, standardobjects: 'List[Tuple[str, str]]' = <factory>, backup_folder_uuids: 'List[str]' = <factory>, has_keyset: 'bool' = False) -> None`**
  One computer-UUID subtree's discovered structure.

- **`def computer_root(root: 'str', computer_uuid: 'str') -> 'str'`**

- **`def discover_layout(backend: 'Backend', root: 'str' = '/', *, concurrency: 'int' = 8, enumerate_objects: 'bool' = True) -> 'List[Arq7ComputerLayout]'`**
  Discover all Arq 7 computer subtrees under ``root``.

- **`def find_latest_backuprecord(backend: 'Backend', root: 'str', computer_uuid: 'str', folder_uuid: 'str') -> 'Optional[str]'`**
  Resolve the absolute path to a folder's most recent backuprecord.

- **`def keyset_path(root: 'str', computer_uuid: 'str') -> 'str'`**

- **`def list_backuprecords(backend: 'Backend', root: 'str', computer_uuid: 'str', folder_uuid: 'str') -> 'List[str]'`**
  Return every backuprecord path under
``backupfolders/<folder>/backuprecords/<bucket>/<num>.backuprecord``,
sorted oldest-first.

- **`def object_path(root: 'str', computer_uuid: 'str', kind: 'str', shard: 'str', file_name: 'str') -> 'str'`**

### `arq_validator.machine_info`

Source-machine identification for an Arq 7 destination.

- **`class HostInfo(hostname: 'str' = '', computer_name: 'str' = '', username: 'str' = '', os_name: 'str' = '', os_version: 'str' = '') -> None`**
  Local-host metadata captured for comparison.

- **`class MachineMatch(computer_name: 'str' = 'unknown', os_type: 'str' = 'unknown', os_version: 'str' = 'unknown') -> None`**
  Diff result of a :class:`SourceMachineInfo` against a
:class:`HostInfo`. Each ``str`` field is one of:

- **`class SourceMachineInfo(computer_uuid: 'str' = '', computer_name: 'str' = '', os_type: 'int' = 0, os_version: 'str' = '', arq_version: 'str' = '', plan_id: 'str' = '', plan_name: 'str' = '', folder_count: 'int' = 0, raw_config: 'dict' = <factory>, raw_plan: 'dict' = <factory>) -> None`**
  Metadata extracted from one ``<computer-uuid>/`` subtree.

- **`def compare(source: 'SourceMachineInfo', host: 'HostInfo') -> 'MachineMatch'`**
  Per-field comparison of source-side metadata against the
current host. Returns a :class:`MachineMatch` summarizing the
overlap.

- **`def read_host_info() -> 'HostInfo'`**
  Capture the current host's identifying metadata. POSIX-only
in practice; Windows lookups fall through to defaults.

- **`def read_source_info(backend: 'Backend', root: 'str' = '/') -> 'List[SourceMachineInfo]'`**
  Return one :class:`SourceMachineInfo` per discovered
computer subtree. Each entry's ``raw_config`` / ``raw_plan``
carry the full sidecar JSON so callers can pretty-print or
inspect any field. ``backend`` need not be open as a context
manager — the caller is responsible for lifecycle.

### `arq_validator.record_validator`

Record-level data validation.

- **`class RecordValidationFailure(blob_id: 'str', rel_path: 'str', offset: 'int', length: 'int', kind: 'str', error: 'str', node_path: 'str' = '') -> None`**
  One blob that failed to fetch or verify. ``loc_path`` /
``loc_offset`` / ``loc_length`` come from the BlobLoc; ``kind``
distinguishes fetch errors from HMAC mismatches from decode
failures so triage can route accordingly.

- **`class RecordValidationReport(record_path: 'str' = '', ok: 'bool' = True, blobs_walked: 'int' = 0, bytes_fetched: 'int' = 0, trees_walked: 'int' = 0, files_walked: 'int' = 0, blobs_skipped_by_ledger: 'int' = 0, elapsed_sec: 'float' = 0.0, truncated_after: 'int' = 0, failures: 'List[RecordValidationFailure]' = <factory>) -> None`**
  Aggregate output of :func:`validate_record`. Operators
typically just check ``ok`` and dump ``failures`` on red.

- **`def validate_record(backend: 'Backend', record_path: 'str', encryption_password: 'str', *, computer_uuid: 'Optional[str]' = None, openssl_path: 'str' = 'openssl', max_blobs: 'int' = 0, callback=None, ledger=None) -> 'RecordValidationReport'`**
  Walk every BlobLoc reachable from ``record_path`` and
verify each one decrypts + passes HMAC.

### `arq_validator.runner`

High-level orchestrator that wires tiers together for one run.

- **`class ValidationReport(tier: 'str' = '', started_at: 'float' = 0.0, finished_at: 'float' = 0.0, root: 'str' = '', backend_kind: 'str' = '', error: 'Optional[str]' = None, layout: 'Optional[LayoutResult]' = None, magic_check: 'Optional[MagicCheckResult]' = None, backuprecord: 'Optional[BackupRecordResult]' = None, audit: 'Optional[ObjectAuditResult]' = None) -> None`**
  Aggregate results from a validation run.

- **`class ValidationTier(*values)`**
  Coarse tier selector matching Arq's own validation hierarchy.

- **`def validate(backend: 'Backend', *, tier: 'ValidationTier' = <ValidationTier.QUICK: 'quick'>, root: 'str' = '/', encryption_password: 'Optional[str]' = None, sample_fraction: 'float' = 0.05, audit_skip_larger_than: 'Optional[int]' = 262144, audit_max_runtime_sec: 'Optional[float]' = None, audit_max_bytes: 'Optional[int]' = None, discover_concurrency: 'int' = 8, magic_concurrency: 'int' = 4, openssl_path: 'str' = 'openssl', callback: 'Optional[ProgressCallback]' = None, audit_ledger=None) -> 'ValidationReport'`**
  Run a full validation at ``tier`` against ``backend``.

### `arq_validator.sftp`

SFTP backend implementing the :class:`~arq_validator.backend.Backend`
protocol over OpenSSH's ssh / sftp client binaries.

- **`class SftpBackend(host: 'str', *, port: 'int' = 22, user: 'str' = '', password: 'Optional[str]' = None, identity_file: 'Optional[os.PathLike]' = None, connect_timeout_sec: 'int' = 30, op_timeout_sec: 'int' = 60, strict_host_key_checking: 'str' = 'accept-new', known_hosts_file: 'Optional[os.PathLike]' = None, ssh_path: 'str' = 'ssh', sftp_path: 'str' = 'sftp', root: 'str' = '', rate_limit_abort_threshold: 'int' = 20, backoff_base_sec: 'float' = 2.0, backoff_max_sec: 'float' = 300.0, backoff_max_attempts: 'int' = 5, backoff_callback: "Optional['Callable[[str, Dict], None]']" = None) -> 'None'`**
  SSH/SFTP-backed implementation of the ``Backend`` protocol.

- **`class SftpConnectionError(...)`**
  Raised when the SSH master cannot be established.

- **`class SftpRateLimitedError(...)`**
  Raised when the backend is hitting a connection-rate limit
on the server side (Hetzner Storage Box being the common one).

### `arq_validator.tiers`

Validation tiers: L0 layout, L1a magic, L1b head HMAC, L2 full audit.

- **`class BackupRecordResult(keyset_decrypted: 'bool' = False, keyset_error: 'Optional[str]' = None, total: 'int' = 0, ok: 'int' = 0, fail: 'int' = 0, failures: 'List[Dict[str, str]]' = <factory>) -> None`**
  L1b outcome: keyset decrypt + per-folder latest-backuprecord HMAC.

- **`class LayoutResult(layout_ok: 'bool' = False, computer_uuids: 'List[str]' = <factory>, blobpack_count: 'int' = 0, treepack_count: 'int' = 0, largeblobpack_count: 'int' = 0, standardobject_count: 'int' = 0, backup_folder_count: 'int' = 0, missing_keyset_for: 'List[str]' = <factory>) -> None`**
  L0 outcome: did we find a valid Arq 7 layout?

- **`class MagicCheckResult(total: 'int' = 0, ok: 'int' = 0, fail: 'int' = 0, failures: 'List[Dict[str, str]]' = <factory>, sample_fraction: 'float' = 1.0) -> None`**
  L1a outcome: ARQO magic-byte sample sweep.

- **`class ObjectAuditResult(files_total: 'int' = 0, files_ok: 'int' = 0, files_fail: 'int' = 0, files_error: 'int' = 0, files_skipped: 'int' = 0, files_skipped_by_ledger: 'int' = 0, inner_arqos_total: 'int' = 0, inner_arqos_ok: 'int' = 0, inner_arqos_fail: 'int' = 0, bytes_read: 'int' = 0, failures: 'List[Dict[str, str]]' = <factory>, aborted_reason: 'Optional[str]' = None) -> None`**
  L2 outcome: full HMAC sweep over every EncryptedObject.

- **`def run_backuprecord_check(backend: 'Backend', layouts: 'List[L.Arq7ComputerLayout]', encryption_password: 'str', *, root: 'str' = '/', openssl_path: 'str' = 'openssl', callback: 'Optional[ProgressCallback]' = None) -> 'BackupRecordResult'`**
  L1b: decrypt each computer's keyset, then HMAC-verify the most
recent backuprecord of every backup folder.

- **`def run_full_audit(backend: 'Backend', layouts: 'List[L.Arq7ComputerLayout]', encryption_password: 'str', *, root: 'str' = '/', skip_larger_than: 'Optional[int]' = 262144, max_runtime_sec: 'Optional[float]' = None, max_bytes: 'Optional[int]' = None, progress_every: 'int' = 100, openssl_path: 'str' = 'openssl', callback: 'Optional[ProgressCallback]' = None, ledger=None) -> 'ObjectAuditResult'`**
  L2: HMAC-verify every EncryptedObject across all object families.

- **`def run_layout_check(layouts: 'List[L.Arq7ComputerLayout]', *, callback: 'Optional[ProgressCallback]' = None) -> 'LayoutResult'`**
  L0: assert at least one computer UUID with at least one object.

- **`def run_magic_check(backend: 'Backend', layouts: 'List[L.Arq7ComputerLayout]', *, root: 'str' = '/', sample_fraction: 'float' = 0.05, concurrency: 'int' = 4, progress_every: 'int' = 500, callback: 'Optional[ProgressCallback]' = None, rng: 'Optional[random.Random]' = None) -> 'MagicCheckResult'`**
  L1a: fetch the first 4 bytes of a sample of object files and
assert they equal ``b"ARQO"``.

## `arq_tui`

### `arq_tui.app`

The top-level Textual application.

- **`class ArqTuiApp(*, config_dir: 'Optional[Path]' = None, plan_registry: 'Optional[PlanRegistry]' = None, destination_store: 'Optional[DestinationStore]' = None, credential_cache: 'Optional[CredentialCache]' = None) -> 'None'`**
  Top-level app. Pushes :class:`HomeScreen` on launch.

- **`def run_app(config_dir: 'Optional[Path]' = None) -> 'int'`**
  Launch the app in the controlling terminal. Returns 0 on a
clean exit; non-zero codes are reserved for future startup
failures (e.g. the config dir is unwritable).

### `arq_tui.backend_open`

Adapter from :class:`~arq_tui.state.Destination` to a live
``Backend`` instance.

- **`def close_backend(backend: 'Backend') -> 'None'`**
  Tear down a backend opened via :func:`open_backend`.

- **`def open_backend(dest: 'Destination', *, sftp_password: 'Optional[str]' = None) -> 'Backend'`**
  Return an opened :class:`Backend` for ``dest``.

### `arq_tui.cli`

Plan-management CLI hosted alongside the TUI launcher.

- **`def main(argv: 'Optional[List[str]]' = None) -> 'int'`**

### `arq_tui.console_commands`

Command dispatch for :class:`~arq_tui.widgets.console.CommandConsole`.

- **`def dispatch_command(app: "'ArqTuiApp'", line: 'str') -> 'str'`**
  Run one command line through the registry.

### `arq_tui.macos_progress`

macOS-only progress + completion notifications.

- **`def is_supported() -> 'bool'`**
  True iff this host can fire the macOS notifications.

- **`def maybe_show_progress(state: '_ProgressState', *, bytes_done: 'int', bytes_total: 'int') -> 'None'`**
  Fire a notification when crossing the next milestone.

- **`def show_complete(plan_name: 'str', *, ok: 'bool', summary: 'str' = '') -> 'None'`**
  Final notification at the end of the run.

- **`def show_start(plan_name: 'str') -> 'None'`**
  One-shot 'backup started' notification.

### `arq_tui.multi_destination`

Run a single :class:`Plan` against every destination it lists.

- **`class MultiBackupResult(plan_id: 'str' = '', plan_name: 'str' = '', started_at: 'float' = 0.0, finished_at: 'float' = 0.0, destinations: 'List[PerDestinationOutcome]' = <factory>) -> None`**
  Top-level result of running one plan against N destinations.

- **`class PerDestinationOutcome(index: 'int', kind: 'str', label: 'str', ok: 'bool', elapsed_sec: 'float' = 0.0, files_written: 'int' = 0, files_reused: 'int' = 0, bytes_plaintext: 'int' = 0, error: 'Optional[str]' = None) -> None`**
  One destination's outcome from :func:`run_plan_multi`.

- **`def run_plan_multi(plan, *, encryption_password: 'str', callback: 'Optional[Callable[[str, dict], None]]' = None, chunker_config=None, exclusions=None, openssl_path: 'str' = 'openssl', backend_factory: 'Optional[Callable[[dict], Any]]' = None, cancel_check: 'Optional[Callable[[], bool]]' = None) -> 'MultiBackupResult'`**
  Run ``plan`` once per destination (in iteration order).

### `arq_tui.notifications`

Pluggable notification on backup completion / failure.

- **`class NotificationConfig(enabled: 'bool' = True, on_status: 'List[str]' = <factory>, desktop_kind: 'Optional[str]' = None, shell_command: 'Optional[str]' = None) -> None`**
  Operator-managed notification settings.

- **`def load_config(path: 'Optional[Path]' = None) -> 'NotificationConfig'`**
  Load operator config from ``path`` or auto-detect.

- **`def notify_run_finished(record, *, config: 'Optional[NotificationConfig]' = None) -> 'Dict[str, bool]'`**
  Fire all configured notifications for a finished run.

### `arq_tui.runs`

State file format + runtime monitoring for backup / restore /
validate operations.

- **`class RunDestination(kind: 'str' = 'local', label: 'str' = '', computer_uuid: 'str' = '') -> None`**
  Coarse description of where the operation is targeting.

- **`class RunEvent(t: 'float', kind: 'str', payload: 'Dict[str, Any]' = <factory>) -> None`**
  One serialized progress event. Keeps the original
``ProgressCb`` ``(kind, payload)`` shape plus a wall-clock
timestamp.

- **`class RunKind(*values)`**
  str(object='') -> str
str(bytes_or_buffer[, encoding[, errors]]) -> str

- **`class RunProgress(files_total: 'Optional[int]' = None, files_done: 'int' = 0, bytes_total: 'Optional[int]' = None, bytes_done: 'int' = 0, current_path: 'str' = '', throughput_bps: 'float' = 0.0, eta_sec: 'Optional[float]' = None) -> None`**
  Live progress counters. ``files_total`` / ``bytes_total``
are ``None`` while planning is in progress (e.g. before the
restore pre-walk emits ``restore_planned``).

- **`class RunRecord(schema_version: 'int' = 1, run_id: 'str' = '', kind: 'str' = 'backup', status: 'str' = 'starting', started_at: 'float' = 0.0, finished_at: 'Optional[float]' = None, pid: 'int' = 0, host: 'str' = '', plan_id: 'str' = '', plan_name: 'str' = '', destination: 'RunDestination' = <factory>, progress: 'RunProgress' = <factory>, result: 'Optional[Dict[str, Any]]' = None, events_tail: 'List[RunEvent]' = <factory>, error: 'Optional[str]' = None) -> None`**
  The on-disk JSON shape, exactly. Field names match the
schema in ``docs/PLAN-cli-tui-split.md``.

- **`class RunStatus(*values)`**
  Coarse lifecycle state. Producers transition starting →
running → (completed | failed | cancelled). The reader marks
a record ``stale`` when its PID has gone away while the
on-disk status was still ``running``.

- **`class RunWriter(record: 'RunRecord', *, state_dir: 'Optional[Path]' = None, flush_interval_sec: 'float' = 1.0, flush_event_count: 'int' = 100) -> 'None'`**
  Producer-side state file manager.

- **`def default_state_dir() -> 'Path'`**
  Return the default state-file directory.

- **`def enumerate_runs(*, state_dir: 'Optional[Path]' = None) -> 'List[RunRecord]'`**
  Read every state file in the runs directory and return the
parsed records, sorted started_at ascending. Files that fail
to parse are skipped (with their path silently discarded — a
half-written record from a crash shouldn't break the listing).

- **`def gc_finished_runs(*, state_dir: 'Optional[Path]' = None, older_than_sec: 'float' = 2592000) -> 'int'`**
  Remove state files for runs that finished more than
``older_than_sec`` ago. Returns the count removed.

- **`def is_pid_alive(pid: 'int') -> 'bool'`**
  Return True iff a process with PID ``pid`` is currently
alive on this host. ``os.kill(pid, 0)`` raises :class:`OSError`
when no such process exists, succeeds otherwise.

- **`def mark_stale(record: 'RunRecord', *, state_dir: 'Optional[Path]' = None) -> 'None'`**
  If a record's on-disk status is ``running`` but its PID is
gone, rewrite it as ``stale``. Idempotent: re-running on an
already-stale record is a no-op.

- **`def new_run_id() -> 'str'`**
  UUID4 stringified — collision-free across hosts. The TUI
looks up runs by ID so they need only to be unique on this
host (writers are sequential per-host).

- **`def run_writer_context(*, kind: 'RunKind', plan_id: 'str' = '', plan_name: 'str' = '', state_file: 'Optional[Path]' = None, state_dir: 'Optional[Path]' = None, run_id: 'Optional[str]' = None)`**
  Convenience: build a :class:`RunRecord` + :class:`RunWriter`
in one call. ``state_file`` overrides ``state_dir`` + ``run_id``
when set (matches the CLI's ``--state-file`` flag).

- **`def signal_cancel(record: 'RunRecord') -> 'bool'`**
  Send SIGTERM to the writer's PID so it can graceful-cancel.
Returns True iff the signal was delivered (PID alive +
permission OK). The writer's exit handler is responsible for
flipping the status to ``cancelled``; this function only
initiates the cancellation.

- **`def state_file_path(run_id: 'str', *, state_dir: 'Optional[Path]' = None) -> 'Path'`**
  Return the absolute path of the state file for ``run_id``.

### `arq_tui.scheduling`

Schedule a saved Plan to run periodically via the host's
native scheduler (crontab on Linux, launchd on macOS).

- **`class ScheduleSpec(cron_expr: 'str' = '', interval_sec: 'int' = 0) -> None`**
  Cadence for a scheduled plan.

- **`def generate_crontab_entry(plan, *, executable: 'Optional[str]' = None, state_dir: 'Optional[Path]' = None, schedule: 'Optional[ScheduleSpec]' = None, password_env_var: 'str' = 'ARQ_BACKUP_PW') -> 'str'`**
  Return one crontab line that runs ``plan``.

- **`def generate_launchd_plist(plan, *, executable: 'Optional[str]' = None, state_dir: 'Optional[Path]' = None, schedule: 'Optional[ScheduleSpec]' = None, password_env_var: 'str' = 'ARQ_BACKUP_PW') -> 'str'`**
  Return the ``Library/LaunchAgents/com.arq-backup-tui.plan-<id>.plist``
XML body that runs ``plan`` via launchd.

- **`def install_gc_schedule(*, crontab_cmd: 'str' = 'crontab', schedule_expr: 'str' = '0 4 * * 0', older_than_days: 'int' = 30, executable: 'Optional[str]' = None) -> 'None'`**
  Install a weekly gc entry that drops finished run-state
files older than ``older_than_days``. Without this, cron-
driven backups accumulate state files indefinitely.

- **`def install_schedule(plan, *, kind: 'str', executable: 'Optional[str]' = None, state_dir: 'Optional[Path]' = None, launch_agents_dir: 'Optional[Path]' = None, crontab_cmd: 'str' = 'crontab', schedule: 'Optional[ScheduleSpec]' = None, password_env_var: 'str' = 'ARQ_BACKUP_PW') -> 'Path'`**
  Install a schedule for ``plan`` in the host's native scheduler.

- **`def list_schedules(*, kind: 'str' = 'auto', crontab_cmd: 'str' = 'crontab', launch_agents_dir: 'Optional[Path]' = None) -> 'List[Tuple[str, str, Path]]'`**
  Return ``[(plan_id, kind, path_or_marker), …]`` for every
arq-backup-tui-managed schedule visible on this host.

- **`def parse_crontab_entries(crontab: 'str') -> 'List[Tuple[str, str]]'`**
  Pull every arq-backup-tui-managed entry out of a crontab
string. Returns ``[(plan_id, full_entry), …]``.

- **`def remove_gc_schedule(*, crontab_cmd: 'str' = 'crontab') -> 'bool'`**
  Drop the gc entry. Returns True iff one was present.

- **`def remove_schedule(plan_id: 'str', *, kind: 'str' = 'auto', crontab_cmd: 'str' = 'crontab', launch_agents_dir: 'Optional[Path]' = None) -> 'int'`**
  Remove the schedule for ``plan_id`` from the host. Returns
the number of entries actually removed (0 means nothing was
installed for that plan).

### `arq_tui.secrets_setup`

Write/refresh ``.secrets/sftp.json`` + ``.secrets/dest_password``
from values the operator just typed into the destination wizard.

- **`def secrets_dir(repo_root: 'Optional[Path]' = None) -> 'Path'`**
  Return the ``.secrets/`` directory path under the repo
root. Default ``repo_root`` walks up from this module's
parent until we hit the repo (the directory containing
``pyproject.toml``).

- **`def secrets_dir_writable(repo_root: 'Optional[Path]' = None) -> 'bool'`**
  Check whether we can create / write into ``.secrets/``.

- **`def write_dest_password(dest_password: 'str', *, repo_root: 'Optional[Path]' = None) -> 'Path'`**
  Write ``.secrets/dest_password`` (the Arq encryption
password — not the SSH password) with mode 0600. Atomic
overwrite of any existing file.

- **`def write_secrets_for_destination(*, host: 'str', user: 'str', port: 'int', root: 'str', identity_file: 'Optional[str]' = None, sftp_password: 'Optional[str]' = None, dest_password: 'Optional[str]' = None, repo_root: 'Optional[Path]' = None) -> 'dict'`**
  One-shot wrapper used by the TUI wizard. Writes whichever
files have content. Returns ``{"sftp_json": path|None,
"dest_password": path|None}`` so the caller can show
"wrote 2 files in .secrets/".

- **`def write_sftp_json(*, host: 'str', user: 'str', port: 'int' = 22, root: 'str', identity_file: 'Optional[str]' = None, sftp_password: 'Optional[str]' = None, write_subdir: 'str' = '.arq-backup-tui-write-test', repo_root: 'Optional[Path]' = None) -> 'Path'`**
  Write ``.secrets/sftp.json`` with the supplied fields.

### `arq_tui.state`

Persistent state for the TUI.

- **`class CredentialCache() -> 'None'`**
  Process-lifetime cache for sensitive credentials.

- **`class Destination(kind: 'str' = 'local', label: 'str' = '', path: 'str' = '', host: 'str' = '', port: 'int' = 22, user: 'str' = '', identity_file: 'str' = '') -> None`**
  A backup destination as remembered by the destination store.

- **`class DestinationStore(*, config_dir: 'Optional[Path]' = None) -> 'None'`**
  File-system-backed list of recently-opened destinations.

- **`class Plan(plan_id: 'str' = '', name: 'str' = '', sources: 'List[str]' = <factory>, destination_kind: 'str' = 'local', destination: 'dict' = <factory>, additional_destinations: 'List[dict]' = <factory>, chunker: 'str' = 'default', per_source_chunkers: 'dict' = <factory>, use_packs: 'bool' = True, dedup_against_existing: 'bool' = True, exclude_globs: 'List[str]' = <factory>, exclude_regexes: 'List[str]' = <factory>, exclude_gitignore_lines: 'List[str]' = <factory>, max_file_bytes: 'Optional[int]' = None, use_apfs_snapshot: 'bool' = False, retention: 'dict' = <factory>, last_run_iso: 'str' = '', schedule: 'dict' = <factory>) -> None`**
  A backup plan record.

- **`class PlanRegistry(*, config_dir: 'Optional[Path]' = None) -> 'None'`**
  File-system-backed plan registry.

### `arq_tui.subprocess_workers`

Subprocess-spawning workers that drive backups via the
``arq-backup`` CLI and watch the state file for progress events.

- **`class SubprocessBackupWorker(target, *, plan, password: 'str', state_dir: 'Optional[Path]' = None, run_id: 'Optional[str]' = None, executable: 'Optional[str]' = None) -> 'None'`**
  Drive a backup by spawning the ``arq-backup create`` CLI.

- **`def subprocess_eligible(plan, dest_kind: 'str') -> 'bool'`**
  Return True iff the subprocess + state-file path can drive
this plan without losing functionality.

### `arq_tui.workers`

Worker-thread bridge for ProgressCb-emitting library calls.

- **`class BackupWorker(target, *, sources, dest_root, encryption_password: 'str', backend=None, computer_uuid: 'Optional[str]' = None, plan_uuid: 'Optional[str]' = None, folder_uuid: 'Optional[str]' = None, use_packs: 'bool' = True, chunker_config=None, dedup_against_existing: 'bool' = True, backup_name: 'str' = 'TUI backup', exclusions=None, max_file_bytes: 'Optional[int]' = None, use_apfs_snapshot: 'bool' = False) -> 'None'`**
  Drive ``arq_writer.build_backup`` from a worker thread.

- **`class MultiDestBackupWorker(target, *, plan, encryption_password: 'str', chunker_config=None, exclusions=None, openssl_path: 'str' = 'openssl') -> 'None'`**
  Drive ``arq_tui.multi_destination.run_plan_multi`` from a
worker thread.

- **`class RestoreWorker(target, *, backend, encryption_password: 'str', folder_uuid: 'str', computer_uuid: 'str', dest, backuprecord_path: 'Optional[str]' = None, paths=None) -> 'None'`**
  Drive ``arq_reader.Restore.restore`` from a worker thread.

- **`class ValidateWorker(target, *, backend, root: 'str' = '/', tier: 'str' = 'quick', password: 'Optional[str]' = None, audit_skip_larger_than: 'Optional[int]' = None, audit_max_runtime_sec: 'Optional[float]' = None, audit_max_bytes: 'Optional[int]' = None) -> 'None'`**
  Drive ``arq_validator.validate`` from a worker thread.

- **`class WorkerEvent(kind: 'str', payload: 'Dict[str, Any]') -> 'None'`**
  One ProgressCb tick translated into a Textual message.

- **`class WorkerFailed(error: 'str', traceback: 'str' = '') -> 'None'`**
  The worker's underlying call raised.

- **`class WorkerFinished(result: 'Any' = None) -> 'None'`**
  The worker's underlying call returned without raising.
